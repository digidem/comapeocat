{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"type": "object",
	"properties": {
		"observation": {
			"type": "array",
			"items": {
				"type": "string",
				"minLength": 1
			},
			"minItems": 1,
			"description": "List of category IDs in the order they should be shown to the user by default for this document type."
		},
		"track": {
			"type": "array",
			"items": {
				"type": "string",
				"minLength": 1
			},
			"minItems": 1,
			"description": "List of category IDs in the order they should be shown to the user by default for this document type."
		}
	},
	"required": [
		"observation",
		"track"
	],
	"title": "Category Selection Schema",
	"description": "Defines the order of categories and which should be shown to the user by default for each document type."
}
